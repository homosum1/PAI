<div class="main__landing">
    <div class="main__landing__description">
        <div class="main__landing__description__header">Oferty dla przetargu: {{auction.title}}</div>

        {{#if offersExceedBudget}}
            <p>Przetarg nie został rozstrzygnięty - wszystkie oferty przekraczają budżet (lub nie istnieją).</p>
        {{else}}
            <div class="main__landing__description__array">
                <table>
                    <thead>
                        <tr>
                            <th>Lp.</th>
                            <th>Składający</th>
                            <th>Wartość Oferty</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each offers}}
                            <tr>
                                <td>{{@index}}</td>
                                <td>{{this.name}}</td>
                                <td>{{this.value}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        {{/if}}
    </div>
</div>